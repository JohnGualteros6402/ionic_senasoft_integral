import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_senasoft_integral',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
